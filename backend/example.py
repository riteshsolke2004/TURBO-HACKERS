# In another file (e.g., main.py)
from main1 import ProductOptimizer

# Create instance (no parameters needed)
optimizer = ProductOptimizer()

# Run the optimization
result = optimizer.run(1985)  # or any product_id
print(result)
